## Local

```bash
cd New-Frontend
npm install
npm run dev
```

### Windows PowerShell

If `npm` is blocked by Execution Policy, use:

```bash
npm.cmd install
npm.cmd run dev
```

Open `http://localhost:3000`.
